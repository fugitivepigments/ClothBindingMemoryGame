import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Publishers Book Cloth Memory Game!</h1>
        <p>Click a cloth but only once or it's GAME OVER!</p>
    </div>
);

export default Title;
